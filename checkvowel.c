#include "stdio.h"
int main()
{
        char alphabet;
        printf("Enter any  Small Alaphabet to check that is vowel or not:-");
        scanf("%c",&alphabet);
        if(alphabet=='a')
        {
            printf("The alphabet is vowel\n");
        }
        else if(alphabet=='e')
        {
            printf("The alphabet is a vowel");
        }
        else if(alphabet=='i')
        {
            printf("The alphabet is vowel\n");
        }
        else if(alphabet=='o')
        {
            printf("The alphabet is vowel\n");
        }
        else if(alphabet=='u')
        {
            printf("The alphabet is a vowel");
        }
        else
        {
            printf("The alphabet is not a vowel");
        }

}
