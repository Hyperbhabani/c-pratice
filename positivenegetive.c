#include "stdio.h"
int main()
{
        int no;
        printf("Enter any no:");
        scanf("%d",&no);
        if(no>=0)
        {
            printf("The no is positive(+ve) no:\n");
        }
        else
        {
            printf("The no is 
                 negetive(-ve) no:\n");
        }
}